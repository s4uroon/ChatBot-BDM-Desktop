# Requirements pour BUILD - ChatBot BDM Desktop
# ===============================================
# Ces dépendances sont nécessaires UNIQUEMENT pour compiler
# l'application en exécutable Windows portable.
#
# Installation :
#   pip install -r requirements.txt
#   pip install -r build_scripts/requirements-build.txt
#
# Puis lancer (depuis la racine du projet) :
#   build_scripts/build_portable.bat
#   ou
#   build_scripts/build_portable.ps1

# PyInstaller pour créer l'exécutable
pyinstaller>=6.0.0

# (Optionnel) Pour optimiser la taille de l'exécutable
# UPX est déjà utilisé par PyInstaller si disponible
